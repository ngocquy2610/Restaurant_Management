<% content_for :title, "New reservation" %>

<div class="min-h-screen bg-[#FBF0EC] px-4 py-10 pt-28">
  <div class="mx-auto max-w-3xl">
    <p class="text-sm font-semibold uppercase tracking-[0.32em] text-[#8A1C2B]/80">Reservations</p>
    <h1 class="mt-3 font-serif text-3xl font-semibold tracking-tight text-gray-900 sm:text-4xl">New reservation</h1>

    <div class="mt-8 rounded-[2rem] border border-gray-200 bg-white p-8 shadow-sm">
      <%= render "form", reservation: @reservation %>
    </div>
  </div>
</div>
