<div class="relative"><%= render "layouts/header" %></div>
<div class="pt-28">
  <h1 class="font-bold text-4xl">Welcome to Lumière Dining</h1>
  <p>A luxury restaurant from Vietnam</p>
  <% @users&.each do |user| %>
    <li>
      <%= user.full_name %> - <%= user.email %>
    </li>
  <%end%>
</div>
